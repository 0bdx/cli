/**
 * The main starter for 0dbx's cli app.
 *
 * @param   {string[]}  argv
 *     Raw command-line arguments, typically Node.js's `process.argv`
 *
 * @return  {string}
 *     Returns the short message appended to "cli(): " [TODO change this]
 */
export function cli(argv: string[]): string;
